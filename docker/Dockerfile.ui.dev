# Development Dockerfile for UI (with hot reload)
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose port (UI runs on 3001)
EXPOSE 3001

# Run development server
CMD ["npm", "run", "dev"]