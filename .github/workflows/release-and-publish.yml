name: Release and Publish

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.2.0
        with:
          go-version: '1.22'

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install OIDC-capable npm
        run: npm install -g npm@12.0.2

      - name: Get version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version $VERSION"

      - name: Update package.json version
        run: |
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" package.json
          echo "Updated package.json to version $VERSION"
          cat package.json | grep version

      - name: Test release sources
        run: |
          go test ./...
          npm test

      - name: Build binaries for all platforms
        run: |
          mkdir -p dist

          COMMIT=$(git rev-parse --short HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          echo "Building binaries for version $VERSION, commit $COMMIT"

          LDFLAGS="-s -w -X github.com/kubeorch/cli/cmd.version=$VERSION -X github.com/kubeorch/cli/cmd.commit=$COMMIT -X github.com/kubeorch/cli/cmd.buildDate=$BUILD_DATE"

          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/orchcli_darwin_amd64 main.go
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/orchcli_darwin_arm64 main.go
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/orchcli_linux_amd64 main.go
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/orchcli_linux_arm64 main.go
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/orchcli_windows_amd64.exe main.go
          CGO_ENABLED=0 GOOS=windows GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/orchcli_windows_arm64.exe main.go

          cd dist
          sha256sum * > checksums.txt
          echo "Checksums:"
          cat checksums.txt
          cd ..

      - name: Create GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          files: dist/*
          generate_release_notes: true
          body: |
            ## OrchCLI ${{ steps.version.outputs.version }}

            ### Installation

            #### Via npm (Recommended)
            ```bash
            npm install -g @kubeorch/cli
            ```

            #### Direct Download
            Download the appropriate binary for your platform from the assets below.

            ##### Linux
            ```bash
            # For Linux x64
            curl -L https://github.com/KubeOrch/cli/releases/download/${{ github.ref_name }}/orchcli_linux_amd64 -o orchcli
            chmod +x orchcli
            sudo mv orchcli /usr/local/bin/

            # For Linux ARM64
            curl -L https://github.com/KubeOrch/cli/releases/download/${{ github.ref_name }}/orchcli_linux_arm64 -o orchcli
            chmod +x orchcli
            sudo mv orchcli /usr/local/bin/
            ```

            ##### macOS
            ```bash
            # For Intel Macs
            curl -L https://github.com/KubeOrch/cli/releases/download/${{ github.ref_name }}/orchcli_darwin_amd64 -o orchcli
            chmod +x orchcli
            sudo mv orchcli /usr/local/bin/

            # For Apple Silicon (M1/M2/M3)
            curl -L https://github.com/KubeOrch/cli/releases/download/${{ github.ref_name }}/orchcli_darwin_arm64 -o orchcli
            chmod +x orchcli
            sudo mv orchcli /usr/local/bin/
            ```

            ##### Windows
            Download `orchcli_windows_amd64.exe` and add it to your PATH.

            ### Verify Installation
            ```bash
            orchcli --version
            ```

            ### Checksums
            SHA256 checksums are available in `checksums.txt` to verify file integrity.

      - name: Prepare npm package
        run: |
          mkdir -p npm/bin
          chmod +x npm/bin/orchcli || true
          echo "Files to be published:"
          npm pack --dry-run

      - name: Publish to NPM
        run: npm publish --provenance --access public
